# Research Change Log

## 2026-04-23

- Reordered `Research Sources.md` and `Research Summaries.md` within each top-level section by date from oldest to newest, adding missing date evidence for previously undated academic and documentation entries before renumbering both files to keep numbering aligned.

- Added `Research Index.md`, a chapter-style topical index for the research folder covering `Research 1.md` through `Research 15.md` plus the bibliography, summaries, and change log files.
- Removed duplicate academic source entry for `An Exploratory Study of Code Retrieval Techniques in Coding Agents` that duplicated the same Preprints.org paper via a version-specific URL.
- Removed duplicate academic source entry for `Structure-Grounded Knowledge Retrieval via Code Dependencies for Multi-Step Data Reasoning` that duplicated the same arXiv paper via a `papers.cool` mirror URL.
- Removed the matching duplicate summary entries from `Research Summaries.md`.
- Renumbered the `Academic Sources` section in both `Research Sources.md` and `Research Summaries.md` so numbering remains sequential within the section after duplicate removal.
- Added new academic source entries and matching summaries for `ReCUBE`, `LocAgent`, `Sufficient Context`, `When the Specification Emerges`, `Beyond Isolated Tasks`, `SWE-Exp`, and `Gistify`.
- Verified that `ContextBench`, `Repoformer`, `SWE Context Bench`, `CoReQA`, `Beyond Code Snippets`, `SWE-QA-Pro`, `FeatureBench`, and `RepoST` already existed in both research files, so no duplicate entries were added for those papers.
- Added new academic source entries and matching summaries for `The Program Dependence Graph and Its Use in Optimization`, `GRACE`, `Evaluating AGENTS.md`, `Empowering RepoQA-Agent based on Reinforcement Learning Driven by Monte-carlo Tree Search`, `A review of software change impact analysis`, `Integrated Impact Analysis for Managing Software Changes`, and `Impact-driven Context Filtering For Cross-file Code Completion`.
- Verified that `GraphCoder`, `Hierarchical Context Pruning`, `CodePlan`, and `SWE Context Bench` already existed in both research files, so no duplicate entries were added for those papers.
- Added new academic source entries and matching summaries for `SWE-Pruner`, `CoSIL`, `A Scalable Benchmark for Repository-Oriented Long-Horizon Conversational Context Management`, `Improving Code Localization with Repository Memory`, `The SWE-Bench Illusion`, and `ATime-Consistent Benchmark for Repository-Level Software Engineering Evaluation`.
- Verified that `ContextBench`, `Impact-driven Context Filtering For Cross-file Code Completion`, `ReCUBE`, `SWE Context Bench`, `When the Specification Emerges`, `Beyond Isolated Tasks`, `FeatureBench`, `RepoST`, `SWE-QA-Pro`, and `Sufficient Context` already existed in both research files, so no duplicate entries were added for those papers.
- Corrected the existing `2504.10046` academic source and summary from the older `CodeRAG` title to the linked v2 title `GraphCodeAgent: Dual Graph-Guided LLM Agent for Retrieval-Augmented Repo-Level Code Generation`, and refreshed the citation metadata.
- Added new academic source entries and matching summaries for `RepoShapley`, `RANGER`, `TDAD`, `Do Not Treat Code as Natural Language`, `Toward Executable Repository-Level Code Generation via Environment Alignment`, and `SGAgent`.
- Verified that `GRACE` already existed in both research files, so no duplicate entry was added for that paper.
- Added new academic source entries and matching summaries for `A Benchmark for Evaluating Repository-Level Code Agents with Intermediate Reasoning on Feature Addition Task`, `SWE-RM`, `Dynamic analysis enhances issue resolution`, and `Are Benchmark Tests Strong Enough? Mutation-Guided Diagnosis and Augmentation of Regression Suites`.
- Verified that `ContextBench`, `Sufficient Context`, `Impact-driven Context Filtering For Cross-file Code Completion`, `SWE Context Bench`, `Improving Code Localization with Repository Memory`, `RepoST`, `SWE-QA-Pro`, and `FeatureBench` already existed in both research files, so no duplicate entries were added for those papers.
- Added a new academic source entry and matching summary for `TypeScript Repository Indexing for Code Agent Retrieval`.
- Verified that `GRACE`, `Do Not Treat Code as Natural Language`, `Impact-driven Context Filtering For Cross-file Code Completion`, `RepoShapley`, `RANGER`, `SGAgent`, `Empowering RepoQA-Agent based on Reinforcement Learning Driven by Monte-carlo Tree Search`, `TDAD`, and `Toward Executable Repository-Level Code Generation via Environment Alignment` already existed in both research files, so no duplicate entries were added for those papers.
- Added new academic source entries and matching summaries for `In Line with Context: Repository-Level Code Generation via Context Inlining` and `CodeMEM: AST-Guided Adaptive Memory for Repository-Level Iterative Code Generation`.
- Verified that `ContextBench`, `Sufficient Context`, `Hierarchical Context Pruning`, `SWE Context Bench`, `When the Specification Emerges`, `A Scalable Benchmark for Repository-Oriented Long-Horizon Conversational Context Management`, `Impact-driven Context Filtering For Cross-file Code Completion`, `Dynamic analysis enhances issue resolution`, `Toward Executable Repository-Level Code Generation via Environment Alignment`, `FeatureBench`, `SWE-QA-Pro`, `The SWE-Bench Illusion`, `Are Benchmark Tests Strong Enough? Mutation-Guided Diagnosis and Augmentation of Regression Suites`, and `Repoformer` already existed in both research files, so no duplicate entries were added for those papers.
- Added new academic source entries and matching summaries for `Stop-RAG: Value-Based Retrieval Control for Iterative RAG` and `Information Gain-based Policy Optimization: A Simple and Effective Approach for Multi-Turn Search Agents`.
- Verified that `GRACE`, `Impact-driven Context Filtering For Cross-file Code Completion`, `RANGER`, `TDAD`, and `RepoShapley` already existed in both research files, so no duplicate entries were added for those papers.
- Added new academic source entries and matching summaries for `Compressing Code Context for LLM-based Issue Resolution` and `IDE-Bench: Evaluating Large Language Models as IDE Agents on Real-World Software Engineering Tasks`.
- Verified that `CodeMEM`, `When the Specification Emerges: Benchmarking Faithfulness Loss in Long-Horizon Coding Agents`, `RepoShapley`, `Dynamic analysis enhances issue resolution`, `FeatureBench`, and `Are Benchmark Tests Strong Enough? Mutation-Guided Diagnosis and Augmentation of Regression Suites` already existed in both research files, so no duplicate entries were added for those papers.
- Left `Documentation` and `Blogs/Articles` unchanged.
